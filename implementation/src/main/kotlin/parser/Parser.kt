package org.example.parser

import org.example.model.Element
import org.example.model.PlxdElement

class Parser {

    fun parse(input: String): List<Element> {
        val stack = ArrayDeque(listOf<Element>())
        return listOf(Element());
    }
}